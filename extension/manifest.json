{
  "manifest_version": 3,
  "name": "P1 One-Click Autofill Engine",
  "version": "0.6.12",
  "description": "Jobright-style one-click autofill for ATS application forms. Deterministic, local-only, no backend, and structurally incapable of submitting.",
  "permissions": ["storage", "activeTab", "scripting", "debugger"],
  "host_permissions": ["<all_urls>"],
  "background": { "service_worker": "background.js", "type": "module" },
  "content_scripts": [
    {
      "matches": ["<all_urls>"],
      "js": ["content.js"],
      "css": ["sidebar.css"],
      "run_at": "document_idle",
      "all_frames": true
    }
  ],
  "web_accessible_resources": [
    { "resources": ["profile.default.json"], "matches": ["<all_urls>"] }
  ],
  "action": { "default_title": "P1 Autofill — open a job application page" },
  "commands": {
    "_execute_action": {
      "suggested_key": {
        "default": "Ctrl+Shift+Y",
        "mac": "MacCtrl+Shift+Y"
      },
      "description": "Open P1 Autofill on the current application page"
    }
  }
}
